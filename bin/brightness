#!/usr/bin/sh

max=$(cat /sys/class/backlight/intel_backlight/max_brightness)
setto=${1:-$((max*2/3))}

echo "max: $max"
echo "set: $setto"
echo $setto | sudo tee /sys/class/backlight/intel_backlight/brightness 1>/dev/null
