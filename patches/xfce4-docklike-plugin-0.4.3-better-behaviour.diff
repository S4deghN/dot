--- src/Dock.cpp	2024-08-09 19:48:59.000000000 +0330
+++ src/Dock.cpp	2026-01-28 23:23:08.573441701 +0330
@@ -154,8 +154,12 @@
					Group* group = (Group*)g_object_get_data(G_OBJECT(widget), "group");

					if (group->mActive)
-						group->scrollWindows(timestamp, GDK_SCROLL_DOWN);
+					{
+						group->mWindows.get(group->mTopWindowIndex)->minimize();
+						group->mTopWindowIndex = (group->mTopWindowIndex + 1) % group->mWindows.size();
+					}
					else if (group->mWindowsCount > 0)
						group->activate(timestamp);
					else
						group->mAppInfo->launch();
